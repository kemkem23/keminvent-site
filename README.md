# keminvent.com

Static single-page site for **keminvent.com**, served via GitHub Pages.

- No JavaScript, no external resources (locked-down `Content-Security-Policy: default-src 'none'`).
- HTTPS enforced by GitHub Pages.
- DNS apex (`@`) on GoDaddy points to GitHub Pages IPs; email (Google MX / SMTP2GO) untouched.
